# tests/e2e/

Testes end-to-end (Playwright) cobrindo os fluxos críticos: descoberta → reserva → pagamento simulado → ingresso digital; onboarding e CRUD do organizador; aprovação de organizador pelo admin global. Rodam contra a stack completa via `infra/docker-compose.yml`.
