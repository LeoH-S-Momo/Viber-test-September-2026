# tests/unit/

Testes unitários (Vitest + React Testing Library) para componentes, hooks e utilitários de `lib/`. Testes co-localizados (`Component.test.tsx` ao lado do componente) também são aceitos; esta pasta é para testes que cruzam múltiplos módulos sem precisar de browser real.
