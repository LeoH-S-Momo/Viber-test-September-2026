# hooks/

React hooks customizados e reutilizáveis entre features (ex: `useDebouncedValue`, `useCabinHold`). Convenção: `useNomeDoHook.ts`.

Hooks específicos de uma única feature devem viver dentro da própria `features/<nome>/`.
