# types/

Tipos TypeScript exclusivos do frontend (ex: `ViewModel`, props de componente, estado de UI). Tipos que representam o contrato com a API vêm de `@seapass/contracts` e não devem ser redeclarados aqui.
