# docs/api/

Destino do `openapi.json` gerado automaticamente pelo `@nestjs/swagger` a partir dos decorators da API (`apps/api`). Não editar manualmente — é artefato gerado; regenerado a cada build via script em `apps/api`. Servido também interativamente em `/docs` pela própria API em desenvolvimento.
